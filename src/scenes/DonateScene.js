import React, { PropTypes } from 'react';
import { View } from 'react-native';

export function DonateScene({ navigator }) {
  return (
    <View />
  );
}

DonateScene.propTypes = {
  navigator: PropTypes.object.isRequired,
};
